{
  "name": "mazza-archive",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 4326",
    "build": "astro build && npm run searchindex && astro build && npm run sitemap",
    "preview": "astro preview --port 4326",
    "sitemap": "python3 ../tools/sitemap.py",
    "searchindex": "python3 ../tools/searchindex.py"
  },
  "dependencies": {
    "astro": "^5.18.2"
  }
}
